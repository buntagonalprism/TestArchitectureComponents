<resources>

    <!-- Base application theme. -->
    <!-- Inheriting any variant of Theme.AppCompat provides support for automatica color-tinting
    our widgets using primay, dark and accent colours -->
    <!-- Light modifier means that we use dark text on light backgrounds -->
    <!-- No action bar means that the standard activity action bar is not included by default, and
    we can include our own support action bar if we so choose within an activity -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. Recall that because these are very recent additions to the
        android attributes list, we use their compatibility version i.e. minus the 'android'
        namespace, and allow the AppCompat theme and activity to apply them as required -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Apply a default style for the colour of title text in toolbars -->
        <item name="titleTextColor">@android:color/white</item>

        <!-- Colour the overflow actions icon the same colour -->
        <item name="actionOverflowButtonStyle">@style/MyOverflowButtonStyle</item>

    </style>

    <style name="MyOverflowButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@android:color/white</item>
    </style>


</resources>
